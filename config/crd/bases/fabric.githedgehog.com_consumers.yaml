---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: consumers.fabric.githedgehog.com
spec:
  group: fabric.githedgehog.com
  names:
    categories:
    - hedgehog
    kind: Consumer
    listKind: ConsumerList
    plural: consumers
    singular: consumer
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Consumer is the Schema for the consumers API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ConsumerSpec defines the desired state of Consumer
            properties:
              kubeCluster:
                description: Foo is an example field of Consumer. Edit consumer_types.go
                  to remove/update
                properties:
                  ports:
                    items:
                      properties:
                        device:
                          type: string
                        port:
                          type: string
                      type: object
                    type: array
                  vlan:
                    properties:
                      id:
                        type: integer
                      untagged:
                        type: boolean
                    type: object
                type: object
            type: object
          status:
            description: ConsumerStatus defines the observed state of Consumer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
