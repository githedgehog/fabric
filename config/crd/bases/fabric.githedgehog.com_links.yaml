---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: links.fabric.githedgehog.com
spec:
  group: fabric.githedgehog.com
  names:
    categories:
    - hedgehog
    kind: Link
    listKind: LinkList
    plural: links
    singular: link
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.ports[0].device
      name: Device0
      type: string
    - jsonPath: .spec.ports[0].port
      name: Port0
      type: string
    - jsonPath: .spec.ports[1].device
      name: Device1
      type: string
    - jsonPath: .spec.ports[1].port
      name: Port1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Link is the Schema for the links API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: LinkSpec defines the desired state of Link
            properties:
              ports:
                items:
                  properties:
                    device:
                      type: string
                    port:
                      type: string
                  type: object
                maxItems: 2
                minItems: 2
                type: array
            required:
            - ports
            type: object
          status:
            description: LinkStatus defines the observed state of Link
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
