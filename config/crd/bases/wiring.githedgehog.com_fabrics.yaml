---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: fabrics.wiring.githedgehog.com
spec:
  group: wiring.githedgehog.com
  names:
    categories:
    - hedgehog
    - wiring
    - fabric
    kind: Fabric
    listKind: FabricList
    plural: fabrics
    shortNames:
    - fab
    singular: fabric
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.asnStart
      name: ASNStart
      type: integer
    - jsonPath: .spec.asnEnd
      name: ASNEnd
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: |-
          Fabric is a single spine-leaf topology, owning its switches, its ASN range and its address and VLAN namespaces.
          Fabrics are not cabled to each other and reach each other by peering as external systems.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec is the desired state of the Fabric
            properties:
              asnEnd:
                description: ASNEnd is the last ASN of the range reserved for this
                  Fabric
                format: int32
                type: integer
              asnStart:
                description: ASNStart is the first ASN of the range reserved for this
                  Fabric
                format: int32
                type: integer
              domains:
                additionalProperties:
                  description: FabricDomainSpec defines a single spine layer of a
                    Fabric
                  properties:
                    spineASN:
                      description: SpineASN is the ASN shared by all spines of this
                        domain, within the Fabric ASN range
                      format: int32
                      type: integer
                  type: object
                description: Domains is the set of spine layers in this Fabric, defaulted
                  to a single "default" domain if empty
                type: object
            type: object
          status:
            description: Status is the observed state of the Fabric
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
