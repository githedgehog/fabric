apiVersion: v1
kind: Service
metadata:
  name: {{ include "controller-proxy.fullname" . }}
  labels:
    {{- include "controller-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      nodePort: {{ .Values.service.nodePort }}
      protocol: TCP
      name: http
  selector:
    {{- include "controller-proxy.selectorLabels" . | nindent 4 }}
