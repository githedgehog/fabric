# permissions for end users to view switchgroups.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: clusterrole
    app.kubernetes.io/instance: switchgroup-viewer-role
    app.kubernetes.io/component: rbac
    app.kubernetes.io/created-by: fabric
    app.kubernetes.io/part-of: fabric
    app.kubernetes.io/managed-by: kustomize
  name: switchgroup-viewer-role
rules:
- apiGroups:
  - wiring.githedgehog.com
  resources:
  - switchgroups
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - wiring.githedgehog.com
  resources:
  - switchgroups/status
  verbs:
  - get
